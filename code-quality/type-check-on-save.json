{
  "enabled": true,
  "name": "TypeScript Type Check on Save",
  "description": "Run tsc --noEmit after editing TypeScript files to catch type errors without producing output",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "**/*.ts",
      "**/*.tsx"
    ]
  },
  "then": {
    "type": "runCommand",
    "command": "npx tsc --noEmit"
  }
}
